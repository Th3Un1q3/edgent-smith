VS Code Dev Containers + OrbStack: forwardPorts never auto-applied in ANY session (tunnel-forwarding activates, zero relays, Ports panel empty) while compose-published ports work. Container restarted 10:29 -> healthy exthost, all extensions load -> 'extensions not loading / no Dev Containers channel / empty Ports panel' in a window = STALE WINDOW in 3h reconnection grace (normal window churn logs 'terminate message from renderer'). Fix order: (1) close ALL stale windows, keep one, Reload Window; (2) Ports panel '+' -> Forward a Port -> 3080 MANUAL test: works -> auto-apply bug (file upstream: forwardPorts not applied, manual OK, healthy exthost, OrbStack 2.2.3); fails -> orb shutdown + restart OrbStack, reload; (3) forwardPorts=[3080] only; publish rest via compose. Upstream class #11772/#10568/#11521/#11707.