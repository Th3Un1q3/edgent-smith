VS Code Dev Containers on OrbStack: dynamic forwardPorts fail while compose-published ports work. Forensics: no forward listeners (ss -tlnp), no tunnel processes, remoteagent.log zero forward lines; tunnel-forwarding extension activates but never creates relays — every session incl. healthy 100-min exthost → client-side relay bug (vscode-remote-release #10568/#11772/#11521/#11707). 'terminate message from renderer' = normal window close, not cause. forwardPorts for sibling-container ports (16686/4318/8080) never forward (#4645 primary-only); 8000/11434 dead (no services); 3080 dsh web IS live, still fails → proves client bug. Fix: rely on compose ports:, prune forwardPorts, file upstream.