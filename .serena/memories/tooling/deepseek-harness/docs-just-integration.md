# dsh just Recipe & Docs Integration

Root justfile gains `just dsh *ARGS` right after `just oc` (both verified). README.md gets a `## DeepSeek Harness (dsh)` section between "Quick start (DevContainer)" and "Autofix workflow", plus a Command map row; md-lint clean.

justfile conventions: mem:tooling/justfile-conventions.