# Memory System Memory Index

The memory-system domain documents the Serena memory system — tools, conventions, and patterns for persisting project knowledge as Markdown files in `.serena/memories/`.

## Memories

| Memory | Description |
|--------|-------------|
| `skills/memory-system/overview` | The 6 Serena memory tools and their behavior, return formats, and error handling |
| `skills/memory-system/conventions` | Naming rules, `mem:` cross-referencing, overview pattern, topic organization |
| `skills/memory-system/write-patterns` | Single writes, batch hierarchies, append patterns, overview+children, defensive write function |
| `skills/memory-system/manage-patterns` | Edit literal/regex, rename with cross-reference updates, delete with guard patterns |

## Cross-References

- `mem:skills/recipe-structure` — how recipes follow memory conventions
- `mem:skills/testing` — live-testing methodology for memory operations
- `mem:refactoring/overview` — refactoring session that produced these memory patterns
