# Skill Root Leanness

Root SKILL.md is the ALWAYS-LOADED surface — every line costs tokens on every trigger, so it must stay lean. Round 3 (2026-08-07) caught a ~46-line cache sub-domain rulebook squatting in the context-gathering root: instance-level mechanics of one sub-domain masquerading as principles.

Root = metadata + triggers + minimal workflow skeleton + ONLY genuinely cross-cutting principles (one-liners with pointers) + routing + related skills. Instance-level operational detail belongs in the reference the pointer names.

Audit: grep the root for sub-domain vocabulary (budget numbers, key schemes, phase labels). Literal rule prose is the smell; routing-table triggers are fine.

Related: mem:skills/general/context-gathering-version-deltas; mem:skills/general/skill-review-generality-actionability; mem:skills/general/skill-principles-active-voice.