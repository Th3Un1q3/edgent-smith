# Conventions Domain Memory Index

The conventions domain documents project-wide coding standards and anti-patterns — Python 3.13 patterns, Click architecture rules, and project structure conventions.

## Memories

| Memory | Description |
|--------|-------------|
| `conventions/python` | Python 3.13 conventions — type annotations, from __future__ imports, modern stdlib |
| `conventions/click-architecture` | Click architecture rules — command routing, service separation |
| `conventions/project-structure` | Package layout and module organization |
| `conventions/anti-patterns` | Anti-patterns to avoid — no src/ layout, no manual venv activation |
| `conventions/justfile` | Justfile conventions and task organization |
| `conventions/agent-design` | Agent design conventions — RUG protocol, skill enforcement |

## Cross-References

- `mem:testing/python-testing` — testing conventions for Python
- `mem:cli/architecture` — CLI conventions
