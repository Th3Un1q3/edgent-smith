# CI/CD Domain Memory Index

The CI domain covers continuous integration and delivery workflows — GitHub Actions configurations and automated quality enforcement.

## Memories

| Memory | Description |
|--------|-------------|
| `ci/github-actions` — GitHub Actions workflow definitions and job configuration |
| `ci/devcontainer-ci` — DevContainer-based CI for consistent execution environments |
| `ci/quality-gates-ci` — Automated quality gate enforcement in CI pipelines |
| `ci/experiment-triggers` — Automated experiment triggers based on issue activity |

## Cross-References

- `mem:quality-gates/configuration` — quality gates that run in CI
- `mem:devcontainers/configuration` — containerized CI environments
