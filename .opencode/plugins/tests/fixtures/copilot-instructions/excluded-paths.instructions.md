---
applyTo: '**/*.ts'
excludePaths: '**/*.test.ts'
---

Instructions that only apply to non-test TypeScript files. This instruction file will not be applied to any files that match the `excludePaths` glob pattern.