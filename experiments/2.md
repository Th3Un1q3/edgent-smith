## Hypothesis

Disabling thinking for the gemma model does not reduce score but reduce speed.

## Mutation surface


- [ ] `agents/edge.py` The code that configures inference when calling the model. Ollama `thinking: off`.
- [ ] `evals/runner.py` models configuration

## Expected eval improvement

Same cases pass and baseline score does not drop.

## Tech nuance

Use ollama model configuration to turn off reasoning. Yet some models may ignore when thinking is set to low. If it's the case, try reducing thinking effort.
