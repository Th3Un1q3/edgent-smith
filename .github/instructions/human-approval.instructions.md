# Performing `<approval_gate>`

To get human/user approval execute the bash command `just approve "What needs approval."`.

## Principles of Approval Requests

NEVER ask for approval in plain text.

If you have written "Approval needed before proceeding." without using the `just approve` command, it means you failed to follow the instruction.